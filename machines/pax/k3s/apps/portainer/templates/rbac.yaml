{{ include "platform.rbac.clusterrole" (dict "app" .Values.apps.portainer "rules" .Values.apps.portainer.rbac.rules) }}
---
{{ include "platform.rbac.clusterrolebinding" (dict "app" .Values.apps.portainer) }}
