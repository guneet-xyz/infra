apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.openwebui.appName }}
  namespace: {{ .Values.apps.openwebui.namespace }}
  labels:
    app: {{ .Values.apps.openwebui.appName }}
spec:
  ports:
    - name: http
      port: {{ .Values.apps.openwebui.port }}
      targetPort: {{ .Values.apps.openwebui.port }}
  selector:
    app: {{ .Values.apps.openwebui.appName }}
