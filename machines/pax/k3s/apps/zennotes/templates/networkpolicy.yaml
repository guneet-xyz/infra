apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: {{ .Values.apps.zennotes.namespace }}
  labels:
    app: {{ .Values.apps.zennotes.appName }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-caddy
  namespace: {{ .Values.apps.zennotes.namespace }}
  labels:
    app: {{ .Values.apps.zennotes.appName }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.apps.zennotes.appName }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.apps.caddy.namespace }}
      ports:
        - port: {{ .Values.apps.zennotes.port }}
          protocol: TCP
