apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.honcho.redis.serviceName }}
  namespace: {{ .Values.apps.honcho.namespace }}
  labels:
    app: {{ .Values.apps.honcho.redis.serviceName }}
spec:
  ports:
    - name: redis
      port: {{ .Values.apps.honcho.redis.port }}
      targetPort: {{ .Values.apps.honcho.redis.port }}
  selector:
    app: {{ .Values.apps.honcho.redis.serviceName }}
