{{ include "platform.rbac.clusterrole" (dict "app" .Values.apps.homepage "rules" .Values.apps.homepage.rbac.rules) }}
---
{{ include "platform.rbac.clusterrolebinding" (dict "app" .Values.apps.homepage) }}
