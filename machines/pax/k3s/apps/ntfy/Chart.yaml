apiVersion: v2
name: ntfy
description: ntfy notification service (Tailscale-only, ntfy.guneet.xyz)
type: application
version: 0.1.0
appVersion: "v2.24"
dependencies:
  - name: platform
    version: ">=0.1.0"
    repository: file://../../shared/platform
