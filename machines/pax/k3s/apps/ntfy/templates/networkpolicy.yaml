apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: {{ .Values.apps.ntfy.namespace }}
  labels:
    app: {{ .Values.apps.ntfy.appName }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-same-namespace
  namespace: {{ .Values.apps.ntfy.namespace }}
  labels:
    app: {{ .Values.apps.ntfy.appName }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.apps.ntfy.appName }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector: {}
      ports:
        - port: {{ .Values.apps.ntfy.port }}
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-caddy
  namespace: {{ .Values.apps.ntfy.namespace }}
  labels:
    app: {{ .Values.apps.ntfy.appName }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.apps.ntfy.appName }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.apps.caddy.namespace }}
      ports:
        - port: {{ .Values.apps.ntfy.port }}
          protocol: TCP
