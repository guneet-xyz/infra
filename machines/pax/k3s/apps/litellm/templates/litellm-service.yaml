apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.litellm.appName }}
  namespace: {{ .Values.apps.litellm.namespace }}
  labels:
    app: {{ .Values.apps.litellm.appName }}
spec:
  ports:
    - name: http
      port: {{ .Values.apps.litellm.port }}
      targetPort: {{ .Values.apps.litellm.port }}
  selector:
    app: {{ .Values.apps.litellm.appName }}
