# Default deny all ingress
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: {{ .Values.apps.headlamp.namespace }}
  labels:
    app: {{ .Values.apps.headlamp.appName }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
# Allow Caddy to reach Headlamp
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-caddy
  namespace: {{ .Values.apps.headlamp.namespace }}
  labels:
    app: {{ .Values.apps.headlamp.appName }}
spec:
  podSelector:
    matchLabels:
      app: {{ .Values.apps.headlamp.appName }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.apps.caddy.namespace }}
      ports:
        - port: {{ .Values.apps.headlamp.port }}
          protocol: TCP
