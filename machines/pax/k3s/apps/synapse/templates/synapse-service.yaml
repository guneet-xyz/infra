apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.apps.synapse.appName }}
  namespace: {{ .Values.apps.synapse.namespace }}
  labels:
    app: {{ .Values.apps.synapse.appName }}
spec:
  ports:
    - name: http
      port: {{ .Values.apps.synapse.port }}
      targetPort: {{ .Values.apps.synapse.port }}
  selector:
    app: {{ .Values.apps.synapse.appName }}
