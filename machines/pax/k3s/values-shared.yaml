# Shared values for cross-app references and common config.
#
# Usage: helm install -f values-shared.yaml -f apps/<chart>/values.yaml ...

apps:
  walls:
    serviceName: walls.walls.svc.cluster.local
    port: 3000
  headlamp:
    serviceName: headlamp.headlamp.svc.cluster.local
    port: 80
  litellm:
    serviceName: litellm.litellm.svc.cluster.local
    port: 4000

smtp:
  host: smtp.azurecomm.net
  port: "587"
  mailFrom: noreply@guneet.xyz
